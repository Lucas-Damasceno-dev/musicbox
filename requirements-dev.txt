# Dependências de desenvolvimento — incluem as de produção e as ferramentas
# de qualidade (testes, cobertura e lint). Todas pinadas (==).
-r requirements.txt
pytest==9.1.1
pytest-cov==7.1.0
httpx==0.28.1
ruff==0.16.1
